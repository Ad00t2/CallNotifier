matrix:
  include:
    - os: windows
    - os: osx
      osx_image: xcode10.2
      language: node_js
      node_js: "10"
    - os: linux
      services: docker
      language: generic

cache:
  directories:
    - node_modules

script:
  - |
    yarn packageAll
before_cache:
  - rm -rf src/dist

branches:
  except:
    - "/^v\\d+\\.\\d+\\.\\d+$/"
